# TRANSLATOR
A Python Tkinter-based language translator that uses GoogleTranslator from the deep_translator library to automatically detect the input language and translate it into the selected language. It features a simple GUI with text input, language selection, and translation output.
